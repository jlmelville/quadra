% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rnn.R
\name{local_radius_correlation}
\alias{local_radius_correlation}
\title{Local Radius Correlation}
\usage{
local_radius_correlation(
  Xin,
  Xout,
  k = 15,
  statistic = c("radius", "mean"),
  method = c("spearman", "pearson"),
  log = FALSE,
  nn_method_in = "nnd",
  metric_in = "sqeuclidean",
  nn_method_out = "brute",
  metric_out = "sqeuclidean",
  is_transposed = FALSE,
  n_threads = 0,
  verbose = FALSE,
  ret_extra = FALSE,
  nn_args_in = list(),
  nn_args_out = list()
)
}
\arguments{
\item{Xin}{the input data (usually high-dimensional), a matrix or data frame
with one observation per row, or if \code{is_transposed = TRUE}, one observation
per column. Alternatively, it can be a pre-computed nearest neighbor graph
with \code{idx} and \code{dist} matrix elements. If \code{Xin} is a data frame,
nonnumeric columns are ignored.}

\item{Xout}{the output data (usually lower dimensional than \code{Xin}), a matrix
or data frame with one observation per row, or if \code{is_transposed = TRUE},
one observation per column. Alternatively, it can be a pre-computed nearest
neighbor graph with \code{idx} and \code{dist} matrix elements. If \code{Xout} is a data
frame, nonnumeric columns are ignored.}

\item{k}{the number of nearest neighbors to use. Can be a numeric vector, in
which case the local radius correlation is calculated for each value
separately.}

\item{statistic}{the local scale statistic. \code{"radius"} uses the distance to
the \code{k}th nearest neighbor. \code{"mean"} uses the mean distance to the first
\code{k} nearest neighbors.}

\item{method}{correlation method, either \code{"spearman"} or \code{"pearson"}.}

\item{log}{if \code{TRUE}, calculate the correlation on log local radii.}

\item{nn_method_in}{the nearest neighbor method to calculate the neighbors of
\code{Xin}. Can be one of \code{"brute"} (brute force calculation) or \code{"nnd"}, the
nearest neighbor descent method of Dong and co-workers (2011).}

\item{metric_in}{the distance calculation to apply to \code{Xin}. One of
\code{"euclidean"}, \code{"sqeuclidean"} (squared Euclidean), \code{"cosine"}, \code{"manhattan"},
\code{"correlation"} (1 minus the Pearson correlation), or \code{"hamming"}. Euclidean
and squared Euclidean have the same exact neighbor ordering, but an
approximate search can still differ because of approximation or ties.}

\item{nn_method_out}{the nearest neighbor method to calculate the neighbors
of \code{Xout}. See \code{nn_method_in} for details.}

\item{metric_out}{the distance metric to apply to \code{Xout}. See \code{metric_in} for
details.}

\item{is_transposed}{if \code{TRUE} then \code{Xin} and \code{Xout} are assumed to have been
passed in transposed format, i.e. with one observation per column.
Otherwise, \code{Xin} and \code{Xout} will be transposed. For large datasets,
transposing can be slow, so if this function will be called multiple times
with the same input data, it is more efficient to transpose the input data
once outside of this function and set \code{is_transposed = TRUE}.}

\item{n_threads}{the maximum number of threads to use. \code{0} or \code{1} runs
serially.}

\item{verbose}{if \code{TRUE}, log information about the calculation to the
console.}

\item{ret_extra}{if \code{TRUE}, additionally return the nearest neighbor graphs
for \code{Xin} and \code{Xout}.}

\item{nn_args_in}{named list of extra arguments for the selected
nearest-neighbor method for \code{Xin}. See Details for naming and routing rules.}

\item{nn_args_out}{named list of extra arguments accepted by the selected
nearest-neighbor method for \code{Xout}, with the same naming and ownership rules
as \code{nn_args_in}.}
}
\value{
A named numeric vector of local radius correlations, one value for
each \code{k}. Items are named \verb{lrc<k>}, where \verb{<k>} refers to the values
provided in the \code{k} parameter. If \code{ret_extra = TRUE}, then a list is
returned containing:
\itemize{
\item \code{nn_in}: the nearest neighbor graph for \code{Xin}.
\item \code{nn_out}: the nearest neighbor graph for \code{Xout}.
\item \code{lrc}: the vector of local radius correlations.
\item \code{scale_in}: a matrix of input local scale values, one column per \code{k}.
\item \code{scale_out}: a matrix of output local scale values, one column per \code{k}.
}
}
\description{
Compares the local scale around each observation in the input data and output
embedding. The local scale is summarized from nearest-neighbor distances,
either as the distance to the \code{k}th nearest non-self neighbor or as the mean
distance to the first \code{k} nearest non-self neighbors.
}
\details{
This is a local scale diagnostic, not a density estimator. A high value means
observations with small or large local radii in the input data tend to have
small or large local radii in the output embedding. It complements
\code{\link[=nn_preservation]{nn_preservation()}}, which measures whether neighbor identities are
preserved.

\code{Xin} and \code{Xout} can be raw observation matrices or pre-computed nearest
neighbor graphs. Unlike \code{\link[=nn_preservation]{nn_preservation()}}, supplied nearest-neighbor graphs
must contain a \code{dist} matrix as well as an \code{idx} matrix because this metric
uses neighbor distances. Graphs calculated by this function are
self-excluded. Older cached self-inclusive graphs are detected and stripped
with a warning.

Supplied graph distances must align with their indices and be ordered
nearest-first. Supported sparse inputs pass through to \code{rnndescent}.

Squared Euclidean preserves neighbor order but changes the distance
magnitudes used by this metric, so its result can differ from Euclidean.

If either local-radius vector is constant, the correlation is undefined and
the corresponding result is \code{NA_real_}. If \code{log = TRUE}, all selected local
radius values must be positive, so duplicate points that produce zero local
radii should be handled before requesting a log-scale comparison.
}
\examples{
iris_pca <- stats::prcomp(iris[, -5], rank. = 2, scale = FALSE, retx = TRUE)$x
local_radius_correlation(
  iris[, -5],
  iris_pca,
  k = 15,
  nn_method_in = "brute"
)

cached <- local_radius_correlation(
  iris[, -5],
  iris_pca,
  k = c(15, 30),
  nn_method_in = "brute",
  ret_extra = TRUE
)
local_radius_correlation(cached$nn_in, cached$nn_out, k = c(15, 30))
}
\seealso{
\code{\link[=nn_preservation]{nn_preservation()}} for neighbor-identity preservation and
\code{\link[=trustworthiness]{trustworthiness()}} for exact rank-penalty neighborhood preservation on
distance matrices.
}

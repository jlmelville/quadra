% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/random-distance.R
\name{random_pair_distance_stress}
\alias{random_pair_distance_stress}
\title{Random Pair Distance Stress}
\usage{
random_pair_distance_stress(
  Xin,
  Xout,
  n_pairs = 1000,
  metric_in = "sqeuclidean",
  metric_out = "sqeuclidean",
  range_scale = TRUE,
  is_transposed = FALSE,
  n_threads = 0
)
}
\arguments{
\item{Xin}{Input data, with observations in rows by default. Data must be
dense and finite; nonnumeric data-frame columns are ignored.}

\item{Xout}{Output data, with the same input conventions and number of
observations as \code{Xin}.}

\item{n_pairs}{Number of pairs to sample.}

\item{metric_in}{Distance metric for \code{Xin}. One of
\code{"euclidean"}, \code{"sqeuclidean"} (squared Euclidean), \code{"cosine"}, \code{"manhattan"},
\code{"correlation"} (1 minus the Pearson correlation), or \code{"hamming"}.}

\item{metric_out}{Distance metric for \code{Xout}. See \code{metric_in} for
details.}

\item{range_scale}{Whether to scale each sampled distance vector to \verb{[0, 1]}.}

\item{is_transposed}{Whether observations are stored in columns rather than
rows.}

\item{n_threads}{Maximum number of threads to use. \code{0} or \code{1} runs
serially.}
}
\value{
Root mean squared difference between the sampled distances.
}
\description{
Returns the root mean squared difference between matched sampled input and
output distances. Each distance vector is scaled to \verb{[0, 1]} by default.
}
\details{
\code{Xin} and \code{metric_in} define the reference geometry. Reset the R seed and
keep \code{n_threads} fixed to reuse the same pairs across calls.
}
\examples{
iris_x <- iris[, -5]
iris_pca2 <- stats::prcomp(iris_x, rank. = 2, scale = FALSE, retx = TRUE)$x
random_pair_distance_stress(iris_x, iris_pca2)
}
\seealso{
\code{\link[=random_pair_distance_correlation]{random_pair_distance_correlation()}},
\code{\link[=random_pair_distance_emd]{random_pair_distance_emd()}}, and \code{\link[=random_triplet_accuracy]{random_triplet_accuracy()}} for other
measures of global structure preservation.
}

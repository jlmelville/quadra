% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/random-distance.R
\name{random_pair_distance_correlation}
\alias{random_pair_distance_correlation}
\title{Random Pair Distance Correlation}
\usage{
random_pair_distance_correlation(
  Xin,
  Xout,
  n_pairs = 1000,
  metric_in = "sqeuclidean",
  metric_out = "sqeuclidean",
  method = c("pearson", "spearman"),
  is_transposed = FALSE,
  n_threads = 0,
  pairs = NULL,
  ret_extra = FALSE
)
}
\arguments{
\item{Xin}{Input data, with observations in rows by default. Data must be
dense and finite; nonnumeric data-frame columns are ignored.}

\item{Xout}{Output data, with the same input conventions and number of
observations as \code{Xin}.}

\item{n_pairs}{Number of pairs to sample.}

\item{metric_in}{Distance metric for \code{Xin}. One of
\code{"euclidean"}, \code{"sqeuclidean"} (squared Euclidean), \code{"cosine"}, \code{"manhattan"},
\code{"correlation"} (1 minus the Pearson correlation), or \code{"hamming"}.}

\item{metric_out}{Distance metric for \code{Xout}. See \code{metric_in} for
details.}

\item{method}{Correlation method, either \code{"pearson"} or \code{"spearman"}.}

\item{is_transposed}{Whether observations are stored in columns rather than
rows.}

\item{n_threads}{Maximum number of threads to use. \code{0} or \code{1} runs
serially.}

\item{pairs}{Optional one-based matrix with one pair per row. The endpoints
in each row must differ. If supplied, \code{n_pairs} is ignored.}

\item{ret_extra}{Whether to return the pairs and their raw distances.}
}
\value{
The correlation, or a list with \code{correlation}, \code{pairs},
\code{distance_in}, and \code{distance_out} when \code{ret_extra = TRUE}.
}
\description{
Correlates distances for sampled pairs of observations in \code{Xin} and \code{Xout}.
Pearson correlation measures linear association; Spearman correlation
measures rank association.
}
\details{
\code{Xin} and \code{metric_in} define the reference geometry. Supply \code{pairs} to reuse
exact comparisons, or reset the R seed and keep \code{n_threads} fixed.
}
\examples{
iris_x <- iris[, -5]
iris_pca2 <- stats::prcomp(iris_x, rank. = 2, scale = FALSE, retx = TRUE)$x
random_pair_distance_correlation(iris_x, iris_pca2)
}
\references{
Becht, E., McInnes, L., Healy, J., Dutertre, C. A., Kwok, I. W.,
Ng, L. G., ... & Newell, E. W. (2019).
Dimensionality reduction for visualizing single-cell data using UMAP.
\emph{Nature biotechnology}, \emph{37}(1), 38-44.
}
\seealso{
\code{\link[=random_pair_distance_emd]{random_pair_distance_emd()}}, \code{\link[=random_pair_distance_stress]{random_pair_distance_stress()}}, and
\code{\link[=random_triplet_accuracy]{random_triplet_accuracy()}} for other global preservation measures.
}

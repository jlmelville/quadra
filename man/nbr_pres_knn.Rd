% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/neighbor.R
\name{nbr_pres_knn}
\alias{nbr_pres_knn}
\title{Neighborhood Preservation Between Nearest Neighbor Matrices}
\usage{
nbr_pres_knn(kin, kout, k = ncol(kin), n_threads = 0)
}
\arguments{
\item{kin}{Reference nearest-neighbor index matrix, with observations in rows
and neighbors in nearest-first order.}

\item{kout}{Nearest-neighbor index matrix to compare with \code{kin}.}

\item{k}{Neighborhood size.}

\item{n_threads}{Maximum number of threads to use. \code{0} or \code{1} runs
serially.}
}
\value{
Per-observation neighborhood overlap in \verb{[0, 1]}.
}
\description{
Returns the overlap between reference and comparison nearest-neighbor
matrices, separately for each observation.
}
\details{
Rows contain distinct one-based indices in nearest-first order. Self-indices
are removed, so self-inclusive matrices need at least \code{k + 1} columns.
Supplied order resolves ties.
}

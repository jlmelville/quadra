% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/neighbor.R
\name{trustworthiness}
\alias{trustworthiness}
\alias{continuity}
\title{Trustworthiness and Continuity Between Distance Matrices}
\usage{
trustworthiness(din, dout, k)

continuity(din, dout, k)
}
\arguments{
\item{din}{Reference distance matrix.}

\item{dout}{Distance matrix to compare with \code{din}.}

\item{k}{Neighborhood size. Must be less than half the number of
observations.}
}
\value{
Scalar rank-preservation score; 1 indicates no penalty.
}
\description{
\code{trustworthiness()} penalizes observations that appear among the \code{k} nearest
neighbors in \code{dout} but have input-space rank greater than \code{k} in \code{din}.
\code{continuity()} applies the dual penalty to input-space neighbors that are no
longer among the \code{k} nearest neighbors in \code{dout}.
}
\details{
Diagonal entries are excluded. Unlike \code{\link[=nbr_pres]{nbr_pres()}}, the penalty reflects how
far a misplaced neighbor falls beyond \code{k}.
Nonfinite entries are ranked rather than rejected; ties follow column order.
}
\examples{
iris_pca <- stats::prcomp(iris[, -5], rank. = 2, scale = FALSE, retx = TRUE)$x
din <- as.matrix(stats::dist(iris[, -5]))
dout <- as.matrix(stats::dist(iris_pca))
trustworthiness(din, dout, k = 15)
continuity(din, dout, k = 15)
}
\references{
Venna, J., & Kaski, S. (2001). Neighborhood preservation in nonlinear
projection methods: An experimental study. In \emph{Artificial Neural Networks -
ICANN 2001} (pp. 485-491).
}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/auc.R
\name{roc_auc}
\alias{roc_auc}
\title{Average ROC AUC for Label Retrieval}
\usage{
roc_auc(dm, labels)
}
\arguments{
\item{dm}{Finite square numeric distance matrix. The query itself is excluded.}

\item{labels}{A label vector without missing values, with one label per row
of \code{dm}.}
}
\value{
A list with components in this order:
\itemize{
\item \code{av_auc}: the mean AUC over defined queries.
\item \code{label_av}: a named list of per-label means in first-appearance order.
}
}
\description{
Ranks other observations by distance from each query and treats matching
labels as relevant.
}
\details{
\code{av_auc} is the mean ROC AUC over defined queries; \code{label_av} gives per-label
means in first-appearance order. Queries without both relevant and irrelevant
observations are excluded. An empty average is \code{NA_real_}.
}
\note{
Requires the \code{PRROC} package.
}
